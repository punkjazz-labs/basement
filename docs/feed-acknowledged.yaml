# Maintainer rulings on upstream drift feed-watch keeps finding.
#
# Each entry matches one finding by recipe, kind, role and the exact
# upstream revision the ruling covered. When upstream moves past that
# revision, the finding opens again and a person looks again. Removing an
# entry also reopens the finding. This file never authorizes a bump; it
# only records that a drift was judged and what the judgment was.
- recipe: qwen36-27b-nvfp4-1s
  kind: source
  revision: eb84ca38d833bb58ab151bde34f2c1f0a1e2f8ed
  reason: >-
    2026-08-23: upstream switched to DFlash speculative decoding on an
    unpinned nightly image with --privileged, then moved to a new
    repository (Qwen3.6-27B-NVFP4-DFlash-DGX-Spark). Our recipe implements
    the method validated at the pinned revision. The DFlash method is a
    tracked candidate for a future recipe version, not this recipe's
    lineage.
- recipe: deepseek-v4-flash-0731-2s
  kind: source
  revision: f104c39a8c6fa68d4ea0b342ad6eed172bc0e635
  reason: >-
    2026-08-23: upstream rewrote its history (the pinned commit and today's
    main share no ancestor) and hardened its stack with new patches, a
    Dockerfile and CI. Our recipe implements the method validated at the
    pinned revision, which still resolves. The hardened stack is a tracked
    candidate for a future recipe version.
# The two laguna rulings recorded here on 2026-08-23 were superseded the
# same night: the owner ordered the rebuilt checkpoint adopted, and recipe
# version 6 pins it, so the drift itself is gone.
